# 0.3.0 源码入库说明

此次入库保持网站功能代码、数据契约和历史 QA 记录不变。新增当前阶段交接文档，移除部署文档中的真实服务器地址和本机身份；只读检查脚本改为从本机参数/环境读取预期目标，并补充此前已验证的进程级 ProgramData 修复。

历史截图留在原交付压缩包，不重复纳入源码树；可使用 scripts/check_ui.py 重生成。单文件预览由 scripts/build_demo.py 从同一组 web 源码构建，生成后纳入仓库。无需转移完整聊天历史。

只排除生成截图和运行期资料，不排除应用运行必需文件。没有 `.env`、数据库、用户会话、真实 Token、密钥、生产日志、第三方安装包或服务器配置。真实服务器信息不在此树中；样例和测试密码仅用于隔离测试，不是部署凭据。

本轮的实际测试与完整性摘要见下方；它们不替代目标环境的部署验收。

## 2026-09-22 入库前复验

- `python -m pytest tests -q`：17 passed in 6.62s（当前隔离开发环境，非目标服务器）。
- `node --check web/app.js`：通过。
- `python -m compileall -q app manage.py scripts examples tests`：通过。
- `bash -n scripts/*.sh`：逐文件语法检查通过。
- 本轮未重新执行浏览器 UI、Docker、服务器部署、TLS 或真实业务测试，历史 QA 不作为这些事项的通过证明。
- 正则敏感标记与待提交文件清单检查：没有发现实际私钥、常见 GitHub/OpenAI Token 或此前服务器 IP；这不是完整安全审计。
